import React, { useState } from "react";

// SubmenuList component
const SubmenuList = ({ items }) =>  {
    return(
        <ul>
            {items.map((item, index) => {
                
            })}
        </ul>
    )
}
